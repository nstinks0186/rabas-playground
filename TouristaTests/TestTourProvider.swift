//
//  TestTourProvider.swift
//  Tourista
//
//  Created by Pinuno Fuentes on 10/13/15.
//  Copyright © 2015 pinunofuentes. All rights reserved.
//

import XCTest
import Tourista

class TestTourProvider: XCTestCase {
    
    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
    
    func tester() {
        var list: [TourProvider] = []
        
        var provider = TourProvider(name: "PhilBooking")
        list.append(provider)
        
        provider = TourProvider(name: "111 Tours and Travel")
        list.append(provider)
        
        provider = TourProvider(name: "8 Wonders Travel & Tours")
        list.append(provider)

        provider = TourProvider(name: "AsiaLight Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Astrokidd Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Attic Tours Philippines, Inc")
        list.append(provider)
        
        provider = TourProvider(name: "Aurora Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Best Calaguas Tour")
        list.append(provider)
        
        provider = TourProvider(name: "Bicol Adventure and Tours Philippines")
        list.append(provider)
        
        provider = TourProvider(name: "Bicol Escapade Tours & Travels")
        list.append(provider)
        
        provider = TourProvider(name: "Boris Travel & Tours Co.")
        list.append(provider)
        
        provider = TourProvider(name: "Calaguas Local")
        list.append(provider)
        
        provider = TourProvider(name: "DOMINUS VOBISCUM TRAVEL AND TOURS Co. (DVTT Co)")
        list.append(provider)
        
        provider = TourProvider(name: "Donsol Eco Tour")
        list.append(provider)
        
        provider = TourProvider(name: "e-Philippines Adventure Travel and Destinations")
        list.append(provider)
        
        provider = TourProvider(name: "GAFA TOURS & TRAVEL SERVICES CORP.")
        list.append(provider)
        
        provider = TourProvider(name: "Gallivant Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "GOLDEN EAGLE TRAVEL & TOURS, INC., (GETTI)")
        list.append(provider)
        
        provider = TourProvider(name: "GoZoom Travel")
        list.append(provider)
        
        provider = TourProvider(name: "Green Earth Tours and Travel")
        list.append(provider)
        
        provider = TourProvider(name: "J8 Travel Services")
        list.append(provider)
        
        provider = TourProvider(name: "Majestic Mount Pinatubo Tour and Homestay")
        list.append(provider)
        
        provider = TourProvider(name: "Mango Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Mayon ATV Tour")
        list.append(provider)
        
        provider = TourProvider(name: "Northlink Events & Travel Services")
        list.append(provider)
        
        provider = TourProvider(name: "Our Restless Feet Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Package Guru")
        list.append(provider)
        
        provider = TourProvider(name: "Parana Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Penano Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "PHFirst Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Redcaterpillar Travel Agency")
        list.append(provider)
        
        provider = TourProvider(name: "Roam International Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Scorpio Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Shadow Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Shore 2 Shore Travel Services")
        list.append(provider)
        
        provider = TourProvider(name: "Sinclair James International Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "SOUTHEAST ASIAN TRAVEL")
        list.append(provider)
        
        provider = TourProvider(name: "Swire Travel Philippines")
        list.append(provider)
        
        provider = TourProvider(name: "Tourific Travel Services")
        list.append(provider)
        
        provider = TourProvider(name: "Travel International Group")
        list.append(provider)
        
        provider = TourProvider(name: "Travel Related Incentive Programmes and Services, Inc")
        list.append(provider)
        
        provider = TourProvider(name: "Travel Ventours")
        list.append(provider)
        
        provider = TourProvider(name: "Travelzone Travle & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Tripods Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Universal Holidays, Inc")
        list.append(provider)
        
        provider = TourProvider(name: "Valenz World Travel")
        list.append(provider)
        
        provider = TourProvider(name: "victor ticyado van rentals & package tours")
        list.append(provider)
        
        provider = TourProvider(name: "Visit 7107 Islands Travel & Tours")
        list.append(provider)
        
        provider = TourProvider(name: "Weekend In Bicol Travel and Tours")
        list.append(provider)
        
        
    }
    
//    func testExample() {
//        // This is an example of a functional test case.
//        // Use XCTAssert and related functions to verify your tests produce the correct results.
//    }
    
//    func testPerformanceExample() {
//        // This is an example of a performance test case.
//        self.measureBlock {
//            // Put the code you want to measure the time of here.
//        }
//    }
    
}
